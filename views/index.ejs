<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Bootstrap, from Twitter</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="">
    <meta name="author" content="">

    <!-- Le styles -->
    <link href="/components/bootstrap/css/bootstrap.css" rel="stylesheet">
    <style type="text/css">
      body {
        padding-top: 60px;
        padding-bottom: 40px;
      }
    </style>

    <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
    <!--[if lt IE 9]>
      <script src="../assets/js/html5shiv.js"></script>
    <![endif]-->

    <!-- Fav and touch icons -->
    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="../assets/ico/apple-touch-icon-144-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="../assets/ico/apple-touch-icon-114-precomposed.png">
      <link rel="apple-touch-icon-precomposed" sizes="72x72" href="../assets/ico/apple-touch-icon-72-precomposed.png">
                    <link rel="apple-touch-icon-precomposed" href="../assets/ico/apple-touch-icon-57-precomposed.png">
                                   <link rel="shortcut icon" href="../assets/ico/favicon.png">
  </head>

  <body>

    <div class="navbar navbar-inverse navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="brand" href="#">likeastore.com</a>
        </div>
      </div>
    </div>

    <div class="container">

      <!-- Main hero unit for a primary marketing message or call to action -->
      <div class="hero-unit">
        <h1>Great news, folks!</h1>
        <p>We are currently working on amazingly cool application and ready to share it with you very soon. Please stay in touch and we'll let you know as sool as we are live. Put you email and click "Notify me" button, just few steps ahead :)</p>
        <p>
          <form class="notify-me form-inline" action="/notify" method="post">
            <div class="control-group">
              <input id="email" type="text" class="input-xxlarge" placeholder="your@email.com">
              <button type="submit" class="btn btn-primary">Notify me</button>
            </div>
          </form>
          <span class="success-message">You've been subscribed!</span> 
        </p>
      </div>

      <hr>

      <footer>
        <p>&copy; likeastore.com 2013</p>
      </footer>

    </div> <!-- /container -->

    <!-- Le javascript
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <script src="components/jquery/jquery.js"></script>
    
    <script type="text/javascript">
      $(function () {
        $('.success-message').hide();

        $('.notify-me').on('submit', function (e) {
          e.preventDefault();

          var $email = $('#email');
          var $group = $email.parent();

          $group.removeClass('error');
          $group.find('span').remove();

          var email = $email.val();
          if (!validateEmail(email)) {
            $group.addClass('error');
            $group.append('<span class="help-inline">Your email looks strange, please correct</span>');
            
            return;
          }

          $.post('/notify', {email: email}, function () {
            $('.success-message').show();
          });
        });

        function validateEmail(email) {
            var re = /\S+@\S+\.\S+/;
            return re.test(email);
        }
      });
  </script>

  </body>
</html>
